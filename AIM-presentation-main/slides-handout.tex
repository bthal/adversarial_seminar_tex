% Handout version of slides.tex: same content, overlays flattened.
%
% Build:  latexmk -pdf slides-handout.tex
%
% Do not edit content here --- this file only sets the option and pulls in
% slides.tex, so the two versions can never drift apart.

\PassOptionsToClass{handout}{beamer}
\input{slides.tex}
